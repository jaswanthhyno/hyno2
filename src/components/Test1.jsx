import React, { useRef } from 'react';
import emailjs from '@emailjs/browser';
import Combainer from './Combainer';

export const Test1 = () => {
  const form = useRef();

  const sendEmail = (e) => {
   
  };

  return (
    <Combainer />
  );
};
